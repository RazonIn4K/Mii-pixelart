# Staging P1 exact-head gate `5a4128f`

**Date:** 2026-07-18 (America/Chicago)

**Target:** `tomodachi-studio-staging`

**Approved source:** `5a4128f88eb6ec71602dc6acd9cdfabe80cc67f3`

**Mode:** community mutations enabled; payment surfaces retired

This is the sanitized completion record for the approved Worker-hosted P1
gate. It contains no secret values, cookies, OAuth identifiers, email or postal
addresses, internal user or creation IDs, object keys, project contents,
request bodies, or raw IP data.

## Exact deployment

The guarded staging release wrapper verified the clean exact source, configured
remote mirrors, private release-readiness record, bindings, migration ledger,
and approved administrator before deploying.

- Cloudflare deployment: `815e0e89-c242-4baa-8a5d-43726008c4d4`
- Active Worker version: `8f478211-0e08-4eff-88c9-1c349318c873`
- Active version number: 20
- Traffic allocation: 100 percent
- Immediate code rollback version: `c56f580f-2238-4775-846d-3d2c08f17c78`
- `COMMUNITY_MUTATIONS_ENABLED=true`
- payment compatibility routes remain provider-free `410 Gone` tombstones

Fresh HTML and API responses identify the approved source and active Worker
version in their sanitized runtime headers. The staging D1, private R2, KV,
Images, Static Assets, and rate-limit bindings remain isolated. The migration
ledger remains `0001` through `0008`; no migration, secret, OAuth setting, role,
DNS record, production resource, or application record was changed by this
gate.

## Functional, crawler, CSP, and accessibility acceptance

The mutations-enabled, credentialless hosted verifier passed 28 allowlisted
requests and 380 assertions. It covered SPA documents, staging canonical
metadata, public JSON envelopes, safe missing-resource documents, Googlebot and
social crawler shells, deny-all `robots.txt`, empty sitemaps, CSP, HSTS, frame
denial, `nosniff`, request IDs, and authoritative
`X-Robots-Tag: noindex,nofollow`. Its fixed same-origin empty-object creation
probe received `401 UNAUTHENTICATED`; the wrong-origin probe received
`403 FORBIDDEN`. Neither probe carried credentials or could create data.

All retired payment paths returned `410 Gone`, JSON, `Cache-Control: no-store`,
and no provider redirect or checkout surface:

- `/api/stripe/products`
- `/api/stripe/create-checkout-session`
- `/api/stripe/session`
- `/api/webhooks/stripe`

Worker-hosted browser acceptance passed:

- 245 public, community, accessibility, CSP, console, navigation, metadata, and
  responsive checks with 98 intentional safety or viewport skips;
- 46 Studio drawing, true-256 canvas, touch, keyboard, reference, import,
  intercepted AI, Copy Guide, and export-failure checks with 192 intentional
  project or viewport skips; and
- the corrected standalone Studio smoke against the live Worker.

There were zero browser-suite failures, console errors, failed application
requests, or horizontal-overflow findings at 1440, 1024, 768, 430, 390, 360,
and 320 pixels. The live Studio snapshot scored 100 in Lighthouse
Accessibility, Best Practices, SEO, and Agentic Browsing, but Lighthouse
correctly found one unweighted label-content mismatch on the visible 4, 8, 16,
and 32px brush buttons.

## Exact-source performance

Chrome traces used a 390 by 844 mobile viewport at DPR 3, Slow 4G, 4x CPU
slowdown, light mode, and isolated browser contexts.

| Surface | Samples | p75 | CLS |
| --- | --- | ---: | ---: |
| Homepage cold LCP | 416, 334, 421, 441, 401 ms | 421 ms | 0.00 |
| Studio start cold LCP | 268, 285, 258, 254, 806 ms | 285 ms | 0.02 |
| Start Blank INP | 75, 82, 84, 90, 87 ms | 87 ms | 0.00 |
| Restored local draft LCP | 307, 370, 332, 318, 297 ms | 332 ms | 0.03 |

The restored-draft run confirmed the private IndexedDB database and one
authoritative editor canvas. The Start Blank run confirmed a native 256 by 256
surface. All measured values remain inside the 2.5-second LCP, 0.1 CLS, and
200-millisecond INP gates.

The production build also passed its bundle budgets at 92.3 KiB gzip initial
JavaScript, 92.3 KiB gzip for the largest chunk, 65.83 KiB gzip for the Studio
chunk, and 30.43 KiB gzip CSS.

## Authenticated AI finding

The configured AI status, model catalog, consent UI, account session, and
intercepted Studio AI application/failure paths passed. The existing staging
account and cloud draft loaded successfully with no console errors. A live,
advice-only request was then stopped by the Worker's intended onboarding guard:
the staged owner account must accept the current Terms version before AI or
other community actions can continue.

The legal attestation was not accepted automatically. No canvas change,
project save, publish action, profile mutation, provider request, D1 write, or
R2 write was performed. Live-provider completion therefore remains the only
user-action-dependent P1 check.

## Corrective candidate

The P1 findings produced a focused local candidate, not deployed by this gate:

- update the standalone Studio smoke to the current true-256 preset names,
  native 256 by 256 import dimensions, starter names, canvas selection, and
  render coordinates;
- add an explicit mutations-enabled mode to the credentialless hosted
  verifier without weakening its default mutations-disabled contract; and
- include the visible `px` text in every brush button's accessible name.

The candidate passed:

- the repository type, license, OpenAPI, migration, game-import, image-import,
  starter, AI-sketch, and resident verifiers;
- eight preflight files and 137 tests;
- 38 Vitest files and 345 tests;
- production Worker and client builds plus bundle budgets;
- 20 focused desktop Studio browser tests;
- five focused WCAG/zoom/keyboard/reduced-motion browser tests;
- the live mutations-enabled hosted verifier and live Studio smoke; and
- a local mobile Lighthouse snapshot with 37 passed audits, zero failures, and
  100 scores in all four reported categories.

## Gate status and production isolation

Exact source `5a4128f88eb6ec71602dc6acd9cdfabe80cc67f3`
remains live only on the isolated staging Worker. `tomodachi.pw` remains on the
Cloudflare Pages application; its session route still returns the legacy HTML
document rather than the Worker JSON contract.

P1 is **not yet production-accepted** because:

1. the owner must accept the current staging Terms update and rerun the
   advice-only live AI request;
2. the corrective candidate must be committed and deployed under a new
   exact-head staging approval; and
3. the short exact-head crawler, Studio smoke, accessibility/Lighthouse, and
   AI confirmation must pass on that candidate before downstream writable and
   production gates use its evidence.

Production, DNS, secrets, roles, OAuth settings, migrations, and application
data were not changed.
