[secrets]
description = "Extend GitLab defaults with Tomodachi provider credentials"
validate = true

[[secrets.passthrough]]
type = "file"
target = "gitleaks.toml"
value = ".gitlab/extended-gitleaks-config.toml"
validator = "toml"
